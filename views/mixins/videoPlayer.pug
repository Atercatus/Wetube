mixin videoPlayer(video = {})
    .videoPlayer#jsVideoPlayer
        video(src=`/${video.src}`)
        .videoPlayer__progressbar
        .videoPlayer__controls
            .videoPlayer__controls-left
                span.play-button#jsPlayButton
                    i.fas.fa-play
                span.volume-button#jsVolumeButton
                    i.fas.fa-volume-up
                div.time-display
                    span#currentTime 00:00:00 
                    span /
                    span#totalTime
            .videoPlayer__controls-right
                span.fullscreen-button#jsFullScreen
                    i.fas.fa-expand